import {
	// call, put, select,
	takeLatest,
} from "redux-saga/effects";

// import * as appActions from 'state/modules/app/actions';

// import ExpensesClient from 'services/api/expenses';

// import { AxiosResponse } from 'axios';
import { AppActionTypes } from "./types";

// export function* handleGetExpenses(): Generator {

// }

export function* appSaga(): Generator {
	// yield takeLatest(AppActionTypes.SIGN_OUT, handleGetExpenses);
}
